# PersonalScripts

A loose collection of scripts I've created and found useful. Not a real project — just a place to keep handy things from disappearing into terminal history.

## What's here

- **claude-profiles.sh** — Claude Code multi-profile launcher (LiteLLM, AWS Bedrock, Subscription). Source it in your `~/.zshrc` to get named commands per backend.
